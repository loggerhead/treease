export const LARGE_FILE_PROCESSING_THRESHOLD_BYTES = 256 * 1024;

export const LARGE_FILE_PROCESSING_INFO =
  'A file is considered large when it is at least 256 KiB (262,144 bytes); it then counts against your large-file processing allowance.';
