name: Main CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

jobs:
  core:
    uses: ./.github/workflows/core.yml

  web:
    uses: ./.github/workflows/web.yml

  cli:
    uses: ./.github/workflows/cli-tests.yml
